# Isolated Tart GuestService qualification

Functional qualification passed on operator host `mac-workstation`, in isolated root `/Users/example/cbt.gX7UO9`, with native VM `cb-bfa2447a4f7cbc2c86c4bd57fb21b1e0`. Production VM state was not modified. A private APFS clone of the production seed was the source. Four CPUs and 8192 MiB RAM were assigned.

The root guest daemon serves typed TLS RPC; PTYs run as dedicated `clankerbox` UID1001, non-admin and without sudo. Recorded negative checks prove private identity binding unreadable and installed daemon executable unwritable by that workload. Input, resize111x37, cursor reconnection, and retained PID/incarnation passed. Graceful stop/start retained a disk token and correctly marked the prior shell LOST; the new shell remained reconnectable with its own stable PID/incarnation.

Persistent host supervision is not yet qualified. On September13 the operator installed the exact system LaunchDaemon, retaining `UserName=gg`, `GroupName=staff`, UID501 and the private socket0600. The foreground service released its lifetime lock and launchd took over; guest dialing still failed with EHOSTUNREACH. Kernel logs identify outgoing bridge100 denial as NECP, while the same destination remains reachable from operator SSH. No TCC or network policy settings were modified.

The earlier assumption that any system-domain job would receive the daemon exemption was incomplete. Apple DTS explicitly distinguishes daemons using `UserName` role accounts. Stable signing alone also did not grant permission: tests covered an explicit ad-hoc identifier, an Apple-issued signature, and a fresh UUID. The current isolated host has an embedded Info.plist naming **Clankerbox Host**, bundle identity `org.clankerbox.host`, an explicit local-network usage description and Apple Development team `F4YX6KHT73`. Its SHA256 is `160d59daa1c773e0d795b20e4fcee5e29d3d900a40bf3d62a393cf4454f2a3a3`. Normal user Local Network permission is pending; source signatures and policy diagnostics remain under `.work/real-local-engine/tart-gate`.

The isolated VM remains running, with original after-cold shell PID768/incarnation `fd7d8f34-da7d-4122-a583-f12ecde17345`. Native label `clankerbox.cb-bfa2447a4f7cbc2c86c4bd57fb21b1e0` remains in `gui/501`. The system host label is `org.clankerbox.tart-gate.cbt.gX7UO9.host`. The prepared `tart-gate probe /Users/example/cbt.gX7UO9` checks that exact session through the host, including reconnection and resize. Do not call `run` again: it creates another VM.

After qualification, `tart-gate delete /Users/example/cbt.gX7UO9` deletes the recorded fixture via normal RPC. The staged root-only `/Users/example/cbt.gX7UO9/uninstall-system.sh` compares the exact owned plist, refuses any undeleted machine record, unloads only that system job and removes only its plist. It retains evidence and needs a separate administrator execution.

Primary Apple references: [TN3179](https://developer.apple.com/documentation/technotes/tn3179-understanding-local-network-privacy), [Apple DTS daemon/UserName clarification](https://developer.apple.com/forums/thread/759955).

## Completed system-service qualification (September13)

The user granted **Clankerbox Host** normal Local Network access. The same signed standalone immediately passed typed TLS/session probing under the system LaunchDaemon, preserving original PID768/incarnation across foreground-to-daemon takeover and a daemon restart. No app wrapper, root execution or blanket policy change was needed.

Cold start then exposed a separate Tart readiness race: native running state preceded availability of the guest-agent exec socket. The product fix waits up to90seconds using only `/usr/bin/true` readiness probes before bootstrap; bootstrap mutations are never retried by that loop. Host restart reconciled the exact unresolved start operation `09bbb3055275ea1c4c747f40ddaba570` to success without a second boot or journal edits. A fresh complete stop/start cycle passed afterward, preserving disk token `dd0e38b8092c0669a63ac9101d3b0717`; the old shell became LOST and the new shell PID747/incarnation `24e8ef4a-c582-4ee6-803b-592373f09330` survived another host restart with typed resume and resize. The normal user permission survived the signed binary/UUID update.

Qualified fixed host SHA256: `38e0604890008c499e7f0037e06ba16706e8b94a8aa09eff57ad128015b3d66e`, same `org.clankerbox.host` Apple signing requirement. Full host/cmd race tests and strict lint passed. The fixture is being stopped/deleted through normal host RPC; only the installed root-owned system plist requires the staged administrator cleanup command. This gate tests the Tart-specific HostService/guest/native path; deployed public controller/proxy qualification remains part of the separate production cutover.
