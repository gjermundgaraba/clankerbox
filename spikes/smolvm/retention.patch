diff --git a/src/agent/manager.rs b/src/agent/manager.rs
index 340eaee3..d04dbcd8 100644
--- a/src/agent/manager.rs
+++ b/src/agent/manager.rs
@@ -538,6 +538,10 @@ pub fn read_egress_telemetry(name: &str) -> Option<u64> {
 
 /// Cache root: `<cache_dir>/smolvm/vms/`.
 pub fn vm_cache_root() -> PathBuf {
+    #[cfg(test)]
+    if let Some(root) = std::env::var_os("SMOLVM_TEST_VM_CACHE_ROOT") {
+        return PathBuf::from(root);
+    }
     dirs::cache_dir()
         .or_else(dirs::data_local_dir)
         .unwrap_or_else(|| PathBuf::from("/tmp"))
@@ -2716,6 +2720,17 @@ impl AgentManager {
     ///
     /// Only safe for ephemeral VMs after the process is confirmed dead.
     pub fn cleanup_data_dir(&self) {
+        if let Some(ref name) = self.name {
+            let Ok(db) = crate::db::SmolvmDb::open() else {
+                return;
+            };
+            if !db
+                .dependent_clones(name)
+                .is_ok_and(|children| children.is_empty())
+            {
+                return;
+            }
+        }
         self.cleanup_cuda_ring_dir();
         if let Some(ref name) = self.name {
             let dir = vm_data_dir(name);
diff --git a/src/api/state.rs b/src/api/state.rs
index 01870ca2..dfcec7c6 100644
--- a/src/api/state.rs
+++ b/src/api/state.rs
@@ -20,14 +20,16 @@ struct CpuSample {
     cpu_time_ns: u64,
 }
 
-/// Records whose disk state must survive startup reconciliation. A stopped or
-/// live clone keeps every ancestor in its qcow2 lineage alive even when an
+/// Records whose disk state must survive startup reconciliation. A persistent,
+/// stopped or live clone keeps every ancestor in its qcow2 lineage alive even when an
 /// ancestor VMM process died; deleting that ancestor's data directory would
 /// silently break the child's backing chain.
 fn retained_fork_lineage(vms: &[(String, VmRecord)]) -> HashSet<String> {
     let mut retained = vms
         .iter()
-        .filter(|(_, record)| record.pid.is_none() || record.is_process_alive())
+        .filter(|(_, record)| {
+            !record.ephemeral || record.pid.is_none() || record.is_process_alive()
+        })
         .map(|(name, _)| name.clone())
         .collect::<HashSet<_>>();
 
@@ -422,7 +424,7 @@ impl ApiState {
                 if retained_lineage.contains(&name) {
                     tracing::warn!(
                         machine = %name,
-                        "machine process died but a retained fork descendant still depends on its disks; preserving it as stopped"
+                        "machine process died; preserving retained workspace or fork ancestor as stopped"
                     );
                     record.pid = None;
                     record.pid_start_time = None;
@@ -434,11 +436,11 @@ impl ApiState {
                     }) {
                         Ok(Some(_)) => {}
                         Ok(None) => {
-                            tracing::warn!(machine = %name, "fork ancestor disappeared during startup reconciliation");
+                            tracing::warn!(machine = %name, "retained machine disappeared during startup reconciliation");
                             continue;
                         }
                         Err(e) => {
-                            tracing::warn!(machine = %name, error = %e, "failed to preserve dead fork ancestor; leaving its data untouched");
+                            tracing::warn!(machine = %name, error = %e, "failed to preserve dead machine; leaving its data untouched");
                             continue;
                         }
                     }
@@ -446,6 +448,7 @@ impl ApiState {
                     tracing::info!(machine = %name, "cleaning up dead machine from database");
                     if let Err(e) = self.db.remove_vm(&name) {
                         tracing::warn!(machine = %name, error = %e, "failed to remove dead machine from database");
+                        continue;
                     }
                     // Reclaim the data dir too. Removing only the DB record leaks the
                     // machine's storage + overlay images (multi-GB sparse files) — and
@@ -561,9 +564,10 @@ impl ApiState {
     /// backing a live DB record is skipped.
     pub fn reclaim_dangling_vm_dirs(&self) -> usize {
         let valid: std::collections::HashSet<String> = match self.db.list_vms() {
-            Ok(vms) => vms
+            Ok(vms) => retained_fork_lineage(&vms)
                 .iter()
-                .map(|(name, _)| crate::agent::vm_dir_hash(name))
+                .chain(vms.iter().map(|(name, _)| name))
+                .map(|name| crate::agent::vm_dir_hash(name))
                 .collect(),
             Err(_) => return 0,
         };
@@ -1952,7 +1956,7 @@ mod tests {
     // ========================================================================
 
     #[test]
-    fn test_load_persisted_machines_removes_dead_records() {
+    fn test_load_persisted_machines_retains_dead_records() {
         let (_dir, state) = temp_api_state();
 
         // Insert a record with a PID that doesn't exist (dead process)
@@ -1961,8 +1965,7 @@ mod tests {
         record.state = RecordState::Running;
         state.db.insert_vm("dead-machine", &record).unwrap();
 
-        // Give it a data dir with a marker file — reconciliation must reclaim it,
-        // not just the DB record (otherwise the disks leak across restarts).
+        // A crash is not permission to discard a persistent workspace.
         let data_dir = crate::agent::vm_data_dir("dead-machine");
         std::fs::create_dir_all(&data_dir).unwrap();
         std::fs::write(data_dir.join("storage.raw"), b"x").unwrap();
@@ -1970,25 +1973,19 @@ mod tests {
         // Verify record exists before load
         assert!(state.db.get_vm("dead-machine").unwrap().is_some());
 
-        // Load should detect dead process and clean up DB record
+        // Exercise both startup cleanup callers, twice (normal restart).
         let loaded = state.load_persisted_machines();
-        assert!(loaded.is_empty(), "dead machine should not be loaded");
-
-        // DB record should be cleaned up
-        assert!(
-            state.db.get_vm("dead-machine").unwrap().is_none(),
-            "dead machine DB record should be removed"
-        );
-
-        // Data dir should be reclaimed too (no disk leak).
-        assert!(
-            !data_dir.exists(),
-            "dead machine data dir should be removed, not leaked"
-        );
-
-        // Name should be available for reuse
+        assert!(loaded.contains(&"dead-machine".to_string()));
+        state.reclaim_dangling_vm_dirs();
+        state.load_persisted_machines();
+        state.reclaim_dangling_vm_dirs();
+        let retained = state.db.get_vm("dead-machine").unwrap().unwrap();
+        assert_eq!(retained.state, RecordState::Stopped);
+        assert_eq!(retained.pid, None);
+        assert_eq!(retained.pid_start_time, None);
+        assert_eq!(std::fs::read(data_dir.join("storage.raw")).unwrap(), b"x");
         let token = SmolvmDb::create_reservation_token();
-        assert!(state.reserve_machine_name("dead-machine", &token).is_ok());
+        assert!(state.reserve_machine_name("dead-machine", &token).is_err());
     }
 
     #[test]
@@ -1996,6 +1993,7 @@ mod tests {
         let (_dir, state) = temp_api_state();
 
         let mut base = VmRecord::new("dead-base".into(), 1, 512, vec![], vec![], false);
+        base.ephemeral = true;
         base.pid = Some(i32::MAX);
         base.state = RecordState::Running;
         state.db.insert_vm("dead-base", &base).unwrap();
@@ -2019,7 +2017,7 @@ mod tests {
     }
 
     #[test]
-    fn test_load_removes_fully_dead_fork_lineage() {
+    fn test_load_retains_fully_dead_fork_lineage() {
         let (_dir, state) = temp_api_state();
         let mut base = VmRecord::new("dead-tree-base".into(), 1, 512, vec![], vec![], false);
         base.pid = Some(i32::MAX);
@@ -2033,8 +2031,54 @@ mod tests {
 
         state.load_persisted_machines();
 
-        assert!(state.db.get_vm("dead-tree-base").unwrap().is_none());
-        assert!(state.db.get_vm("dead-tree-child").unwrap().is_none());
+        assert_eq!(
+            state.db.get_vm("dead-tree-base").unwrap().unwrap().state,
+            RecordState::Stopped
+        );
+        assert_eq!(
+            state.db.get_vm("dead-tree-child").unwrap().unwrap().state,
+            RecordState::Stopped
+        );
+    }
+
+    #[test]
+    fn retention_ephemeral_cleanup_and_explicit_delete() {
+        let (_dir, state) = temp_api_state();
+        for (name, ephemeral) in [("retention-ephemeral", true), ("retention-delete", false)] {
+            let mut record = VmRecord::new(name.into(), 1, 512, vec![], vec![], false);
+            record.pid = Some(i32::MAX);
+            record.state = RecordState::Running;
+            record.ephemeral = ephemeral;
+            state.db.insert_vm(name, &record).unwrap();
+            std::fs::create_dir_all(crate::agent::vm_data_dir(name)).unwrap();
+        }
+        state.load_persisted_machines();
+        assert!(state.db.get_vm("retention-ephemeral").unwrap().is_none());
+        assert!(!crate::agent::vm_data_dir("retention-ephemeral").exists());
+        state.remove_machine("retention-delete").unwrap();
+        state.reclaim_dangling_vm_dirs();
+        assert!(state.db.get_vm("retention-delete").unwrap().is_none());
+        assert!(!crate::agent::vm_data_dir("retention-delete").exists());
+    }
+
+    #[test]
+    fn retention_sweep_protects_missing_ancestor_record() {
+        let (_dir, state) = temp_api_state();
+        let mut child = VmRecord::new(
+            "retention-orphan-child".into(),
+            1,
+            512,
+            vec![],
+            vec![],
+            false,
+        );
+        child.golden = Some("retention-missing-base".into());
+        state.db.insert_vm(&child.name, &child).unwrap();
+        let base = crate::agent::vm_data_dir("retention-missing-base");
+        std::fs::create_dir_all(&base).unwrap();
+        std::fs::write(base.join("storage.raw"), b"backing").unwrap();
+        state.reclaim_dangling_vm_dirs();
+        assert_eq!(std::fs::read(base.join("storage.raw")).unwrap(), b"backing");
     }
 
     #[test]
diff --git a/src/cli/cleanup_ephemeral.rs b/src/cli/cleanup_ephemeral.rs
index 82bef24d..65ad23e2 100644
--- a/src/cli/cleanup_ephemeral.rs
+++ b/src/cli/cleanup_ephemeral.rs
@@ -17,6 +17,13 @@ use std::path::Path;
 
 /// Entry point for the `_cleanup-ephemeral` subcommand.
 pub fn run(vm_name: &str, pid: i32, start_time: u64) {
+    // Ephemeral exit cannot authorize deleting a retained child's backing disks.
+    let Ok(db) = smolvm::db::SmolvmDb::open() else {
+        return;
+    };
+    if !matches!(db.dependent_clones(vm_name), Ok(children) if children.is_empty()) {
+        return;
+    }
     let data_dir = vm_data_dir(vm_name);
     let cache_root = vm_cache_root();
     // The ephemeral DB record is keyed by the VM's own name (see `RunCmd::run`),
diff --git a/src/cli/vm_common.rs b/src/cli/vm_common.rs
index 71a2e821..a01b567e 100644
--- a/src/cli/vm_common.rs
+++ b/src/cli/vm_common.rs
@@ -2989,7 +2989,11 @@ fn orphaned_ephemeral_names(
         if out.len() >= limit {
             break;
         }
-        if !record.ephemeral {
+        if !record.ephemeral
+            || vms
+                .iter()
+                .any(|(_, child)| child.golden.as_deref() == Some(name.as_str()))
+        {
             continue;
         }
         let is_orphan = match record.pid {
@@ -3036,10 +3040,9 @@ pub fn cleanup_orphaned_ephemeral_vms_bounded(limit: usize) {
     };
     for name in orphaned_ephemeral_names(&vms, smolvm::process::is_alive, limit) {
         tracing::debug!(name = %name, "cleaning up orphaned ephemeral VM");
-        let _ = db.remove_vm(name);
         let dir = smolvm::agent::vm_data_dir(name);
-        if dir.exists() {
-            let _ = std::fs::remove_dir_all(&dir);
+        if let Err(error) = remove_vm_data_and_record(&db, name, &dir) {
+            tracing::warn!(name, %error, "failed to clean orphaned ephemeral machine");
         }
     }
 }
@@ -3179,6 +3182,18 @@ mod init_runner_tests {
         assert!(orphaned_ephemeral_names(&vms, alive, 0).is_empty());
     }
 
+    #[test]
+    fn orphaned_ephemeral_names_preserves_fork_ancestry() {
+        let mut base = VmRecord::new("base".into(), 1, 256, vec![], vec![], false);
+        base.ephemeral = true;
+        base.pid = Some(10);
+        let mut child = VmRecord::new("child".into(), 1, 256, vec![], vec![], false);
+        child.golden = Some("base".into());
+        child.pid = Some(11);
+        let vms = vec![(base.name.clone(), base), (child.name.clone(), child)];
+        assert!(orphaned_ephemeral_names(&vms, |_| false, usize::MAX).is_empty());
+    }
+
     #[test]
     fn failed_data_removal_preserves_machine_record() {
         let dir = tempfile::TempDir::new().unwrap();
