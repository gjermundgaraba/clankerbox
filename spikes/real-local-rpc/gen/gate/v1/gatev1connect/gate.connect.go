// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gate/v1/gate.proto

package gatev1connect

import (
	v1 "clankerbox/spikes/real-local-rpc/gen/gate/v1"
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TerminalProbeName is the fully-qualified name of the TerminalProbe service.
	TerminalProbeName = "gate.v1.TerminalProbe"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TerminalProbeAttachProcedure is the fully-qualified name of the TerminalProbe's Attach RPC.
	TerminalProbeAttachProcedure = "/gate.v1.TerminalProbe/Attach"
	// TerminalProbeGetStatsProcedure is the fully-qualified name of the TerminalProbe's GetStats RPC.
	TerminalProbeGetStatsProcedure = "/gate.v1.TerminalProbe/GetStats"
)

// TerminalProbeClient is a client for the gate.v1.TerminalProbe service.
type TerminalProbeClient interface {
	Attach(context.Context) *connect.BidiStreamForClient[v1.AttachmentRequest, v1.AttachmentEvent]
	GetStats(context.Context, *connect.Request[v1.GetStatsRequest]) (*connect.Response[v1.GetStatsResponse], error)
}

// NewTerminalProbeClient constructs a client for the gate.v1.TerminalProbe service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTerminalProbeClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TerminalProbeClient {
	baseURL = strings.TrimRight(baseURL, "/")
	terminalProbeMethods := v1.File_gate_v1_gate_proto.Services().ByName("TerminalProbe").Methods()
	return &terminalProbeClient{
		attach: connect.NewClient[v1.AttachmentRequest, v1.AttachmentEvent](
			httpClient,
			baseURL+TerminalProbeAttachProcedure,
			connect.WithSchema(terminalProbeMethods.ByName("Attach")),
			connect.WithClientOptions(opts...),
		),
		getStats: connect.NewClient[v1.GetStatsRequest, v1.GetStatsResponse](
			httpClient,
			baseURL+TerminalProbeGetStatsProcedure,
			connect.WithSchema(terminalProbeMethods.ByName("GetStats")),
			connect.WithClientOptions(opts...),
		),
	}
}

// terminalProbeClient implements TerminalProbeClient.
type terminalProbeClient struct {
	attach   *connect.Client[v1.AttachmentRequest, v1.AttachmentEvent]
	getStats *connect.Client[v1.GetStatsRequest, v1.GetStatsResponse]
}

// Attach calls gate.v1.TerminalProbe.Attach.
func (c *terminalProbeClient) Attach(ctx context.Context) *connect.BidiStreamForClient[v1.AttachmentRequest, v1.AttachmentEvent] {
	return c.attach.CallBidiStream(ctx)
}

// GetStats calls gate.v1.TerminalProbe.GetStats.
func (c *terminalProbeClient) GetStats(ctx context.Context, req *connect.Request[v1.GetStatsRequest]) (*connect.Response[v1.GetStatsResponse], error) {
	return c.getStats.CallUnary(ctx, req)
}

// TerminalProbeHandler is an implementation of the gate.v1.TerminalProbe service.
type TerminalProbeHandler interface {
	Attach(context.Context, *connect.BidiStream[v1.AttachmentRequest, v1.AttachmentEvent]) error
	GetStats(context.Context, *connect.Request[v1.GetStatsRequest]) (*connect.Response[v1.GetStatsResponse], error)
}

// NewTerminalProbeHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTerminalProbeHandler(svc TerminalProbeHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	terminalProbeMethods := v1.File_gate_v1_gate_proto.Services().ByName("TerminalProbe").Methods()
	terminalProbeAttachHandler := connect.NewBidiStreamHandler(
		TerminalProbeAttachProcedure,
		svc.Attach,
		connect.WithSchema(terminalProbeMethods.ByName("Attach")),
		connect.WithHandlerOptions(opts...),
	)
	terminalProbeGetStatsHandler := connect.NewUnaryHandler(
		TerminalProbeGetStatsProcedure,
		svc.GetStats,
		connect.WithSchema(terminalProbeMethods.ByName("GetStats")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gate.v1.TerminalProbe/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TerminalProbeAttachProcedure:
			terminalProbeAttachHandler.ServeHTTP(w, r)
		case TerminalProbeGetStatsProcedure:
			terminalProbeGetStatsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTerminalProbeHandler returns CodeUnimplemented from all methods.
type UnimplementedTerminalProbeHandler struct{}

func (UnimplementedTerminalProbeHandler) Attach(context.Context, *connect.BidiStream[v1.AttachmentRequest, v1.AttachmentEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("gate.v1.TerminalProbe.Attach is not implemented"))
}

func (UnimplementedTerminalProbeHandler) GetStats(context.Context, *connect.Request[v1.GetStatsRequest]) (*connect.Response[v1.GetStatsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gate.v1.TerminalProbe.GetStats is not implemented"))
}
